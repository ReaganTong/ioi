<ContentPage
    x:Class="mobile_app.Views.QuizPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:mobile_app.ViewModels">

    <ContentPage.BindingContext>
        <viewmodels:QuizViewModel />
    </ContentPage.BindingContext>

    <CollectionView ItemsSource="{Binding Quizzes}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Border StrokeThickness="0" Margin="10,5" BackgroundColor="{Binding Color}" StrokeShape="RoundRectangle 15">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer 
                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:QuizViewModel}}, Path=PlayQuizCommand}"
                    CommandParameter="{Binding .}" />
                    </Border.GestureRecognizers>

                    <Grid ColumnDefinitions="60, *" Padding="20">
                        <Label Text="❓" FontSize="30" VerticalOptions="Center" HorizontalOptions="Center"/>
                        <VerticalStackLayout Grid.Column="1" Spacing="5">
                            <Label Text="{Binding Title}" FontAttributes="Bold" FontSize="18" TextColor="White" />
                            <Label Text="{Binding Description}" FontSize="13" TextColor="#F0F0F0" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
